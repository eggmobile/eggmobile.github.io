source "https://rubygems.org"

# Use the same gem set as GitHub Pages for local preview.
gem "github-pages", group: :jekyll_plugins

# Ensure CSV stdlib gem is available inside Bundler environment
gem "csv"
gem "bigdecimal"
gem "webrick"